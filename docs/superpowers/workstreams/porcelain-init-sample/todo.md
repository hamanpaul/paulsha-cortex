---
status: accepted
work_item: porcelain-init-sample
---

# porcelain-init-sample Todo

## Tasks

- [x] 將 issue #93、active OpenSpec change `porcelain-init-sample` 與本 Todo 綁定為同一 confirmed Work Item。
- [x] coordinator 派工 copilot（gpt-5.4）完成 `init-sample` 命令（TDD）。
- [x] ForeignReview（claude/sonnet）、自動 push/PR、bot review、merge 與 archive 閉合。
- [x] pipx 重裝後 `cortex init-sample --task "..."` 產出 `dispatch: hold` 的 spec，且輸出的必補欄位清單與 `deck verify` 命令可讓不熟悉 deck 概念的使用者獨立完成下一步。
