---
status: accepted
work_item: agy-builder-support
---

# agy-builder-support Todo

## Tasks

- [x] 追認 provisioned worktree 才是 agy builder 的 fail-safe 判定；planner 的
      `read_only=True` 形態即使帶 `write_forbidden` 也維持 `plan+sandbox` 且不附
      `--add-dir`。本項僅記錄 operator 核可的 pre-archive 規格細化；archive、merge、
      issue closure 與 done 仍由 Manager 後續處理。
