# Decision log for the #1029 proposal

**State:** draft proposal; it is not accepted as a whole and changes no production behavior. Individual owner determinations relayed on 2026-09-24 are recorded in the decision table. Vectors remain synthetic contract fixtures, not implementation evidence.

## Live issue evidence read

The current bodies were fetched from GitHub on 2026-09-24; the issues reported zero comments at fetch time unless otherwise changed in the live bodies.

- [#978](https://github.com/hamanpaul/paulsha-cortex/issues/978) fixes the aggregate boundaries and says #1029 freezes schema only; #992 owns `workflow.py`, #993 `registry.py`, #994 `github_delivery.py`, #979 brainstorm/plan producer, #980 PR producer. It also keeps #847 AC10 with #965.
- [#992](https://github.com/hamanpaul/paulsha-cortex/issues/992) fixes the 14-key envelope, `J/H`, `receipt_id`, producer kinds, authority digest meaning, three event/publication formulas, legacy parsing expectations, and the exact plan snapshot path and accepted-plan selection rule. Its 2026-09-24 addendum binds planning transaction kind to each intent schema and requires the trusted-current-run/claim proof before reusing `source_revision` as authority digest.
- [#993](https://github.com/hamanpaul/paulsha-cortex/issues/993) owns only `registry.py`; its 2026-09-24 amendment requires a nonempty one-event batch to be wholly new or exact full-batch replay, with one persist attempt and no partial write.
- [#994](https://github.com/hamanpaul/paulsha-cortex/issues/994) fixes authenticated read-only GET, exact witness/read-back tuple, marker grammar, metadata normalization, and the no-creator-proof boundary.
- [#979](https://github.com/hamanpaul/paulsha-cortex/issues/979) fixes accepted brainstorm inputs, ordered plan selection, snapshot timing, bounded operation proof needs, and prohibits new `step_id` or journal engine.
- [#980](https://github.com/hamanpaul/paulsha-cortex/issues/980) fixes PR intent contents/boundaries, successful POST witness requirement, metadata digest normalization, and ambiguous lost-response behavior.
- [#982](https://github.com/hamanpaul/paulsha-cortex/issues/982) fixes create/adopt provenance: only a successful structured POST is `created`; existing PRs and ambiguous retries are not Manager-created.
- [#983](https://github.com/hamanpaul/paulsha-cortex/issues/983) fixes delivery-journal conditional-write ownership and forbids result-only or stale-write acceptance; it does not own receipt schema or PR creation.
- [#1029](https://github.com/hamanpaul/paulsha-cortex/issues/1029) lists the remaining nested schema, path/hash, diagnostics, duplicate-key, and vector decisions and requires owner alignment before #992 proceeds.

## Production shapes inspected in the checkout

The checkout was `main` at `origin/main` and already had unrelated untracked plan files. They were left unchanged.

- `paulsha_cortex/coordinator/workflow.py:379-412,660-805`: `WorkflowRun` currently stores `planning_authority`, `planning_source_revision`, `pr_refs`, attempts, and gate refs; it has no `publication_receipts` field. Existing `GateEvidenceRef.to_dict()` is `{kind, ref}` plus optional `sha256` (`workflow.py:275-310`).
- `paulsha_cortex/coordinator/planning.py:74-75,351-402,1266-1275,1648-1805,2124-2135`: `_hash_payload` is ordinary SHA256 over compact sorted JSON; `_make_question` derives the `q-` ID from `{kind,prompt,source_refs}`; `_build_default_question_pack` uses the ordered missing kinds and same-kind-ref/accepted-ref fallback, then hashes the question rows for `qp-`; `validate_question_pack` requires exact equality with that deterministic report pack after normalization. `CompletenessReport.to_dict()` stores ordered assessments/missing kinds; peer evidence is a JSON object with scope, question pack, secondary evidence, primary integration, and artifact rows `{kind, ref, sha256}`. `manager.py:8154-8166` writes peer evidence as sorted compact JSON plus one trailing LF and hashes those exact bytes.
- `paulsha_cortex/coordinator/manager.py:3167-3233`: plan materialization selects the first accepted `kind="plan"` assessment, requires one output pattern, derives one canonical target, then writes via the planning transaction. It currently has a crash window before the registry update, which #979 addresses.
- `paulsha_cortex/coordinator/manager.py:7959-8170`: the brainstorm transaction journal has mutable `phase`, absolute paths, rollback content, modes, and `created_dirs`; an operation currently includes `kind,path,before_exists,before_hash,before_content,before_mode,after_hash,after_mode,created_dirs,mutation`. The proposal's operation is deliberately a bounded identity projection rather than a digest of that mutable recovery journal, as required by #992/#979.
- `paulsha_cortex/coordinator/work_bridge.py:606-628,2023-2047` and `github_delivery.py:886-980`: ship metadata is currently materialized as title/body/labels, `create_or_get_pull_request()` may adopt an existing PR, returns only an integer, and then `ensure_pr_metadata()` can PATCH/PUT remote metadata. That current path cannot supply #980's structured POST witness and is why the proposal treats the new PR contract as a future owner-owned seam.
- `paulsha_cortex/coordinator/registry.py:617-635,1881-2002`: registry create/update reconstructs `WorkflowRun` from known fields and `_persist()` writes the full registry. Existing `workflow.py` parsing cannot recover duplicate keys after `json.loads`; a raw-aware row decoder is needed for #1029's duplicate-key history case.

## Decisions and rationale

| ID | Proposal choice | Issue evidence | Current shape / reason | Owner decision still needed |
|---|---|---|---|---|
| D1 | Keep #992's 14 envelope keys verbatim. No optional/null envelope fields. | #978 and #992 list the exact envelope and ban extension keys. | `WorkflowRun` has no receipt slot yet; the contract must not invent a producer provenance flag. | #992 accepts D1; whole-proposal acceptance remains pending. |
| D2 | Raw SHA256 for exact artifact/evidence/snapshot/intent bytes; domain-separated `H` for structured IDs/facts; `pre_publication_authority_sha256` is the distinct captured WorkAuthority digest and is never substituted with source revision or either other hash category. Snapshot/intent sidecar bytes are `J(core)` without a trailing LF. | #992 fixes `J/H` and WorkAuthority provenance semantics; #1029 asks raw-versus-canonical digest basis; #980/#994 define the structured metadata hash. | Brainstorm peer evidence retains producer-defined compact JSON plus its trailing LF; the immutable receipt sidecars use a separate canonical byte rule. Raw bytes, structured values, and authority digest have different inputs. | The exact authority digest is checked by the trusted producer under #979/#980; it is not derived by this parser. |
| D3 | Exact case-sensitive identity strings, no general Unicode normalization; canonical repo spelling is `owner/name`; only PR title/labels use NFC, labels deduplicate after NFC then sort, and PR body only normalizes line endings. | #992 requires exact repo/work/run/claim binding. #980/#994 scope NFC to title/labels and line-ending normalization to body; #980's label rule says normalize, sort, and deduplicate. | `GateEvidenceRef.ref` is a path string, `run.repo` is its own identity spelling, and current GitHub code validates owner/name characters without case folding. | #992 accepts D3; #979/#980 own producer-specific input binding. |
| D4 | Coordinator sidecars use repository-independent relative paths; brainstorm GateEvidenceRef keeps its current absolute path; bounded proof uses explicit `path_domain` plus relative `ref`. Percent-encode run/card as a UTF-8 URI segment with uppercase hex. | #992 fixes plan snapshot path grammar and ref retention; #979 distinguishes workspace paths from coordinator evidence; #1029 asks path domains/encoding. | Brainstorm evidence locator is absolute and inside coordinator root; transaction operations are absolute host paths, so the proposal projects them to their owning-root-relative refs. | #992/#979 accept the proposed snapshot/operation path shape; #993 accepts exact ref round-trip. |
| D5 | Brainstorm snapshot duplicates both question packs and preserves full ordered artifact SOURCE TEXT plus raw-byte hashes alongside assessment facts. | #992 requires completeness report, default/accepted packs, artifact facts, define attempt; #979 requires full snapshot content evidence. | Current report stores assessment facts but not source contents; peer evidence stores final artifact hashes. | #979 accepts the brainstorm snapshot/content shape; the vector is a proposed fixture, not producer implementation evidence. |
| D6 | Plan snapshot holds every ordered assessment with source text/hash, selected index, exact `phase/card/phase_attempt`, sole declared output, and exact target facts. Receipt `accepted_input` points to selected source; it does not replace retained source text. | #992 addendum fixes `planning-input-snapshots/<run>/plan/<attempt>/<card>.json`, no `step_id`, and first accepted plan in order. #979 requires all ordered assessments and target facts. | `manager._materialize_plan_card_output()` selects first accepted plan and derives the one output path. | #992/#979 accept this snapshot/selection shape. |
| D7 | Bounded operation row has exact keys `path_domain,ref,kind,before_exists,before_sha256,after_sha256,mutation`; exclude rollback bytes, mode, dirs, and phase. Sort by `(path_domain,ref,kind,after_sha256)`. | #992 defines bounded proof; #979 requires before/after state, workspace artifact mutation, and same journal evidence operation. | Recovery journal carries absolute paths and mutable rollback fields; proposal projects only the stable operation identity. | #979 accepts the snapshot/operation details relayed by root. |
| D8 | Brainstorm event includes run identity, authority digest, accepted snapshot byte hash, peer evidence ref/hash, and question pack ID. Each output publication hashes `(event ID, exact selected artifact operation)`. | #992 defines the brainstorm event/publication basis; #979 binds it to one source/evidence transaction. | Current transaction groups multiple artifacts plus evidence, so one event and per-output IDs preserve batch semantics. | #992 accepts D8; #979 confirms source/evidence join. |
| D9 | Plan event hashes ordered acceptance facts including assessment projections, phase/card/attempt, pattern, and one-match target. Publication hashes the exact target object. | #992 fixes exact plan identity components; #979 requires ordered assessments and selected source. | Producer already has these facts; no `step_id` is added. | #992 accepts D9; #979 confirms producer-side facts. |
| D10 | PR intent schema is `cortex-manager-pr-intent/v1`; retain normalized request metadata and its digest in canonical intent core. Put the metadata digest in `published_object` only after full GET equality. | #992 fixes PR intent domain/core; #980 fixes normalized metadata digest; #994 requires authenticated read-back and separates observation from creator proof. | Current PR metadata file is not a canonical sidecar; the immutable intent makes restart checks reproducible. | #980/#994 accept the metadata core / observation split. |
| D11 | PR witness is exactly `{repository,number,id,node_id}`. GET observation has nested `head`/`base`, state `open`, exact marker, and metadata digest only after full comparison. Bind witness repository to intent target and successful POST response, not GET. | #982 requires successful POST creator provenance; #994 requires tuple equality/read-back; #980 requires durable witness. | Current create API returns only a number and can adopt existing PRs, so current path cannot populate this evidence. | #980/#994 accept witness/GET shapes and POST-repository binding. |
| D12 | Planning transaction `kind` equals its intent schema literal; brainstorm=`cortex-planning-publication-intent/v1`, plan=`cortex-plan-materialization-intent/v1`; PR=`cortex-manager-pr-intent/v1`. | #992 addendum fixes planning literals; #980 fixes PR intent sidecar schema. | The receipt parser checks producer/transaction-kind pairing locally; resolver checks referenced schema. | #992 accepts planning literals; #980 accepts PR literal. |
| D13 | Unknown/malformed receipt rows use fixed wrapper codes and a zero-based non-bool integer index; invalid containers remain explicit, and missing legacy field means empty. Multi-defect precedence follows #992 D13: raw duplicate row; unknown schema; unknown producer kind; malformed known v1; invalid digest; cross-binding. | #992 fixes tolerant row parse, raw/diagnostic preservation, type/range rules, and deterministic precedence; #993 isolates duplicate receipt rows before map construction. | Existing `WorkflowRun.from_dict` rejects many malformed core fields, so the new history needs a dedicated tolerant boundary. | #992 confirms D13's precedence and index rule; #993 owns registry integration. |
| D14 | Detect decoded-name duplicate JSON members before map construction. Preserve a duplicate only inside `workflows[i].publication_receipts[j]` as exact UTF-8 `raw_json` with index `j`; load sibling workflows. Any duplicate outside that row fails the whole registry load. | #992 requires duplicate-key history to remain readable and invalid; #993 accepts receipt-row isolation and outside-row fail-closed loading. | Existing `_load` calls `json.loads` before `WorkflowRun.from_dict`, so implementation needs a raw-aware registry boundary. Golden fixture is a complete v2 document with two loadable workflows. | #993 accepts the load/recovery boundary; implementation remains outside #1029. |
| D15 | Batch append is wholly new or exact full-batch replay at the current revision; any invalid/opaque registry receipt history blocks append. A stale revision conflicts even if the batch otherwise exactly replays. | Live #993 amendment and owner determination. | Whole-batch validation precedes the one CAS persistence. | #993 accepts D15. |
| D16 | The brainstorm vector derives the production `default_question_pack` from an accepted plan at `workstreams/demo/todo.md`, missing `spec` then `design`, and fallback refs; accepted pack repeats the exact default. It uses the existing compiled output patterns for task slug `contract-1029`. | #979 requires real accepted-input facts; #992 requires the default/accepted packs; #1029 requires literal golden vectors. | `planning.py:351-402,1266-1275` determines the pack; current manifest/compile sources determine output patterns. | #992/#979 accept the reachable deterministic default fixture. |
| D17 | Literal append vectors cover two-output all-new/exact replay, stale-revision replay, partial replay, duplicate pair, mixed event, same event with changed valid payload, cross-event publication-ID reuse, same planning key conflict, different run/claim positives, different producer-key positive, invalid history, and cross-history PR resource conflict. | #1029 requires negative/multi-output/replay coverage; #993 amendment fixes whole-batch semantics and owner determination fixes planning/PR resource keys. | Generator carries complete receipts where these producer values are valid and exact keys for the producer-variant comparator. | #993 accepts the key/replay matrix. Distinct valid same-ID SHA256 collision remains unconstructible without a real cryptographic collision. |
| D18 | `publication_receipts` round-trip preserves exact invalid-row, invalid-JSON-row, and invalid-container wrappers; missing field and `[]` both become empty; unknown wrappers remain opaque and append-conflicting. | #992 defines tolerant row parse and wrapper preservation; #993 requires malformed history to block append. | Pure reload never fetches sidecars; duplicate key source is captured only at raw registry load. | #992/#993 accept wrapper shapes and preservation semantics. |
| D19 | Literal PR negatives cover each witness/read-back tuple field, candidate/intent mismatch, metadata digest mismatch, and raw marker grammar; CRLF/CR GET bodies pass after line-ending normalization. Include NFC title/label normalization, marker-bearing rendered body, and generic `{}`. | #980 fixes normalized metadata and marker exclusion; #994 fixes exact authenticated GET tuple/body equality and GET line-ending normalization; #1029 requires literal vectors. | `get_response_raw_body_unicode` is the unmodified JSON-decoded `pull["body"]` string; current delivery API cannot provide the witness. | #980/#994 accept the marker, witness, metadata, and line-ending rules. |
| D20 | Separate pure receipt parsing from resolver validation. Receipt parser checks shape/types, IDs/digests and contained local bindings; an independent pure PR intent-value validator recomputes formulas only after the exact referenced sidecar has been fetched and parsed. `WorkflowRun.from_dict` never fetches sidecars or recomputes the PR intent core from a receipt that lacks it. | #992 addendum assigns value parser shape/IDs/digests/binds and leaves trusted WorkAuthority-origin proof to #979/#980; #993 owns append/load behavior. | Resolver runs before private producer append; typed-valid reload means provenance-unverified. | #992/#993 accept the value-parser/resolver split. |
| D21 | Brainstorm requires workspace artifact mutation, unique same-kind/ref/hash peer artifact row, primary-integration source text/hash, exact `{repo,work_id,source_revision}` peer scope, and one GateEvidenceRef plus evidence operation joined through the same persisted journal operation. Brainstorm may overwrite an existing authority-owned artifact if before-state is truthful; plan target must be new. | #979 requires bounded operation/evidence join, captured-run scope, and new plan target; #992 separates contained authority digest equality from trusted-origin proof. | `manager.py` validates peer schema/scope and journals before/after state; no transaction ID is invented. | #979 accepts the scope, source, operation and sidecar snapshot rules relayed by root. |
| D22 | Planning key is `(run_id, claim_key, producer_kind, canonical_workspace_ref)`; PR resource key is `(canonical_repository, positive_number)` across valid registry history. Same-run/claim/producer/ref with a different event conflicts; exact full-batch replay is allowed only with identical payloads/patch at current revision. | #993 live body requires same run/claim/producer/object-ref conflict; #979 withdrew the broader repo/work/claim/ref proposal and finalized this narrow key; #993/#994 bind PR resource identity to POST target/witness and GET confirmation. | Same ref in a different run, claim era, or producer variant does not collide. Different `claim_key` on same ref is allowed. | #979/#993 confirm the narrow planning key; #993/#994 confirm the PR key. |

## Remaining review items

The owner determinations relayed to root on 2026-09-24 are incorporated above: #992's value-parser and contained-binding boundary; #993's invalid-history, decoded duplicate-key, append/CAS and exact planning/PR key behavior; #994's JSON-decoded GET body, marker scan, normalization and observation semantics; #979's source-retaining snapshots, evidence/journal join, overwrite-vs-new-target rules and final narrow key; and #980's immutable intent, POST witness and delivery ordering. This records issue-specific choices, not acceptance of the appendix as a whole.

1. **Receipt ID collision vector:** a pair of distinct valid SHA-256 preimages with the same `receipt_id` cannot be generated without an actual cryptographic collision. The vector stays pending and does not fake an ID; #993's required behavior remains to reject a duplicate ID before mutation.
2. **Overall proposal review:** the whole appendix is still draft/unaccepted. Individual determinations above do not claim owner sign-off on this combined document or its generated values.

Vectors are proposal fixtures, not implementation evidence. WorkAuthority trusted-origin checks, producer sidecar resolution, full registry raw-span recovery, append/CAS behavior, and PR delivery ordering have not been exercised against production code in this docs-only task.
