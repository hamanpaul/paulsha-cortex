- **#929 executor backoff slice consumers**：slice lane 的 `dispatch_ready` 現在會在 `_record_pending_slice`／worktree／launch／job 建立前先查 durable executor-backoff 狀態；命中 active cooldown 時不建立 pending slice、worktree、job 或模型 session。`run fanout`／`run tick`／dispatch request／`retry-build` 會透過 `dispatch_skipped_by_backoff` 回 known-vs-unknown skip：已知 cooldown 帶 `retry_after_epoch`，store unknown 只帶 `reason` 與 `retry_after_epoch: null`，且 unknown 不代表 quota 已可用。#825 的 quota pool／forecast／reservation（R9）仍未完成。
