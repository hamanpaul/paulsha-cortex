- **#695：新增 generated asset attestation inventory 與 installed-runtime drift compare**，輸出 Manager／Monitor／job unit、shim、polkit rule、gitconfig、Copilot toolchain wrapper 與 Manager GitHub credential surfaces 的 deterministic hash / 安裝 metadata；JSON 投影固定 redacted、不輸出 asset 內容，Copilot wrapper 以單一 shell argument 引用 pinned target 並要求絕對 system Node binary，comment-only text drift 降為 warning，verify-dispatch 的 reviewer sandbox 在同 UID/direct 模式下不再冗餘要求 `setfacl`，且 official OpenSpec archive 現在固定經過 purpose backfill wrapper，`generated-asset-attestation` 的 archived canonical spec 不再殘留 `Purpose: TBD` stub。
