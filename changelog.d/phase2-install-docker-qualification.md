`verdict_spool_dir` 現在要求既存的實體目錄並拒絕 symlink ancestor，避免 foreign-review launcher 的 sandbox grant 被父層 redirect 導出 Manager-owned spool；同時保留 review-only／relative／leaf-symlink fail-closed contract。

修正 Tests workflow 的 OS fixture：每個 Python matrix job 先安裝 `acl`，並建立 trust-root 四個 system accounts 與其 account-owned、runner 可解析的 `0755` HOME；因此 CI 與實機 deployment 的 `setfacl`／`pwd` 前提一致，不再以缺少帳號造成大量假紅（production trust-root HOME 仍為 `0700`）。

`#665` 的 srt/openspec service tool wrapper 現在固定使用 `/usr/bin/node --jitless`，維持 reviewer 與 Manager unit 的 `MemoryDenyWriteExecute=yes`；rootless regression 驗證 `unresolved_node_execution_surfaces()` 為空，受保護 RC 仍需在實際 systemd unit 驗證。

修正 Phase 2 Docker RC qualification：R9 攻擊矩陣依 trust-root `writer_accounts` 驗證合法 producer 寫入，並把 registry 推導的父層 traverse ACL 納入 transactional install plan；`review-verdict` 明確列為 Phase 2a legacy deny-only asset，權威通道維持 `review-verdict-spool`；job-visible manager-only `handoff-manifest` 與 legacy verdict probe 各自使用隔離 parent，避免 builder default ACL 與 reviewer delete capability 汙染，rootless Docker probe restore 採合成 owner 還原；T3 probe restart 前重置 systemd start-rate counter；reference container 在首次 apply 建立帳號後，以非機密 root-owned legacy Codex control fixture（保留 installer 產生的 enforcement `hooks.json`，scaffold 後清除 canonical 與 legacy placeholder credential authority）執行 production `trust_root scaffold`，並在 protected Codex credential import 後再次執行 production scaffold，將新憑證投影到 Manager-owned canonical authority；第二次 scaffold 僅短暫重建 reviewer 的非機密 optional placeholder 並在完成後清除；gate legal-job probe 使用 production raw job identity→template instance 推導，並透過 `prepare_commit_spool()`、`provision_runtime_surfaces()` 與 `prepare_job_log_spool()` 以 exact `%i` 預建 commit、monitor event、Codex home/cache 與 canonical log surfaces；AGY 1.1.18 preflight 改驗證官方 machine-readable `/quota` payload，拒絕 malformed 或 exhausted quota；Codex 0.149.0 preflight 改走 pinned app-server JSON-RPC `account/read` 與 `account/rateLimits/read`，拒絕以 `doctor --json` 推論登入或 quota；Copilot 1.0.80 preflight 改走 pinned headless SDK server JSON-RPC `account.getCurrentAuth` 與 `account.getQuota`，拒絕 absent 或 exhausted snapshots；新增 exact-plan、授權 mutation、provider schema 與 legacy 邊界 regression tests。
