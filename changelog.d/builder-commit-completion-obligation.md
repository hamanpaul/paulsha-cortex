### Added
- **Issue #340：builder persona 契約新增 `completion_obligations`（結束前必須 commit）**：`PersonaContract` 新增 `completion_obligations: tuple[str, ...] = ()` 欄位（`skills` 之後、frozen dataclass、有預設值不破壞既有建構呼叫），`validate_persona_schema`／`load_catalog` 同步 fail-closed 檢查型別（非字串清單一律拒載）。`personas.yaml` 的 `builder` 角色（`version` bump 至 `2.1.0`）新增一條義務：「完成前 MUST 執行 git add 加入本次全部變更並 git commit；worktree 不乾淨即視為未完成，不得回報 passed／完成」，由 `render_contract_prompt` 注入 dispatch prompt（`coordinator/contract_command.py` → `coordinator/autonomy.py` manager 自主 fan-out 實際呼叫路徑），補上「事前宣告義務」這一環；空清單角色（`manager`／`planner`／`reviewer`）不受影響。既有 `commit_policy: required`（deck cards.yaml）只管 launcher 寫入權限、與 manager 端 `candidate-worktree-dirty`／`candidate-not-advanced` 事後安全網皆維持不變，三者互補不重疊。
