### Fixed
- **Issue #366：install service 身分守衛改比對身分真值，PSC_REPO_ROOT 補上守衛——F44 復發修復（#148 未完成的一半）**：`deploy/installer.py` 新增 env key `PSC_REPO_IDENTITY`，由 `_resolve_repo_identity()` 從 `git remote origin` 正規化（`_normalize_git_origin()`：SSH `git@host:owner/name.git` 與 HTTPS `https://host/owner/name` 視為同一身分）而來，非 git repo／無 origin 一律 fail-safe 退回 `path:<resolved repo_root>`。移除 `#198` 遺留的「既有 PY 比對呼叫者」守衛（`_resolve_python_executable` 一併移除），改為「既有 `PSC_REPO_IDENTITY` 比對新解析出的身分」——比對基準從「呼叫者是誰」換成「這個 instance 該治理什麼」，解掉腐化一次後守衛永久失效的根因（F44 復現測試：既有值已腐化、呼叫者恰好與腐化值同源時仍能擋住）。新增 `--rebind` 顯式繞過旗標（`install_service_result` / `install_service` / `main()` 三層皆加，keyword-only 預設 `False`，向後相容），合法搬遷情境不需手動改 env 檔；`porcelain/service.py` 對稱透傳 `--rebind`，避免外部呼叫端（如 paulshaclaw 的 `start.sh`）被迫繞去裸 `cortex install service`。遷移路徑：既有 env 缺 `PSC_REPO_IDENTITY`（本機四個 instance 現況）視為空字串短路放行並補寫戳記，不 fail-closed，避免現役安裝下次 install 時集體壞掉。錯誤訊息補上 env 檔實際路徑（`installer.py:208` 已算出的 `env_file`，之前漏帶）。`doctor.py` 新增 `_repo_identity_probe`（`repo-identity`），比對 `PSC_REPO_ROOT` 目前實際解析出的身分與記錄的 `PSC_REPO_IDENTITY` 是否一致：drift 時 `fail`（`required=True`，能在潛伏期內讓 `cortex doctor` 轉紅）、戳記缺席（舊安裝）與 `PSC_REPO_ROOT` 未設時皆為 `warn`（`required=False`，不阻斷過渡期）。同步修正 `README.md:597` 與新語意矛盾的舊敘述。遷移期仍有殘餘風險窗（見 PR 說明）：若補寫戳記的那一次 install 恰好就是誤指的呼叫者，該次仍會把錯的身分當真值寫入，需靠儘快讓現役 instance 跑過一次新版 install 縮短暴露窗口。
