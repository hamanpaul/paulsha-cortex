### Changed
- **lifecycle 詞彙表改為治理平面與記憶平面的聯集，修復 `claim`／`research` 分歧造成的跨平面對齊 FAIL**：`persona/contract.PHASES` 由 7 個擴充為 8 個，新增 hippo 的首階段 `research`，成為 `("claim", "research", "define", "plan", "build", "verify", "review", "ship")`。背景：`ae4bc43` 把首階段由 `research` 改名為 `claim`，但 `claim` 在 cortex 是**結構上不同的階段**——`coordinator/workflow.py:300` 強制 `steps[0].phase == "claim"`，`coordinator/work_actions.py:1337-1342` 把該階段釘成 manager 決定性執行（`executor="cortex-manager"`、`model="deterministic"`、`gate_result="passed"`），語意是「認領 work item」而非「研究問題」；hippo 的 `research` 則是記憶 slice 的調查階段，兩者不可互相改名。由於 `13192b4` 已剪除 hippo lifecycle import（本 repo 對 hippo 維持零 runtime 依賴），兩平面各自持有常數、由消費端 paulshaclaw 的 `tests/test_cortex_alignment.py::test_cortex_phases_match_hippo_schema` 守相等性，該測試自此分歧起即 FAIL。改採聯集後兩平面詞彙逐字相等，且不需在任一側改名或遷移資料。取捨依據：`PHASES` 在兩平面**都只做成員資格檢查**（本 repo 為 `contract.py:66/114/169` 的 `phase not in PHASES` 與 `allowed_phases` 子集驗證；hippo 為 `schema.py:90`、`events.py:40`），不決定順序，因此聯集不影響任何既有行為；真正的執行序列是另一個常數 `coordinator/workflow.WORKFLOW_PHASES`，維持 7 個、自 `claim` 起跑不變。新增 `tests/test_phases_constant.py::test_workflow_phases_stays_the_executed_pipeline` 釘住「詞彙表擴充不得外溢到執行管線」這條界線。異動此表必須同步 `paulsha-hippo` 的 `lib/lifecycle/schema.PHASES`，已在常數註解寫明。
